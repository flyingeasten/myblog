# ================
# 1. Hugo 相关（必须忽略）
# ================
# Hugo 生成的静态文件目录（Netlify 会自动构建，无需提交）
public/
# Hugo 构建时生成的锁文件（防止重复构建，本地临时文件）
.hugo_build.lock
# 本地调试用的配置（如果有，比如包含本地路径的 config.local.toml）
config.local.toml
# 主题的临时缓存（如果主题有缓存目录）
themes/*/node_modules/  # 若主题依赖 npm 包，忽略 node_modules


# ================
# 2. Obsidian 相关（方案2必加）
# ================
# Obsidian 的配置目录（包含插件、样式、设置，无需跟踪）
# .obsidian/
# Obsidian 生成的缓存/临时文件
.obsidian.vimrc
.obsidian.workspace
# 未发布的草稿（如果你的 Obsidian 库中有单独的草稿文件夹，比如叫 drafts）
# drafts/
# Obsidian 附件目录（如果你的图片/附件已通过脚本同步到 Hugo 的 static 目录，可忽略）
# attachments/


# ================
# 3. 通用系统/编辑器文件（推荐加）
# ================
# 操作系统自动生成的文件
.DS_Store        # Mac 系统
Thumbs.db        # Windows 系统
# 编辑器临时文件（以 VS Code 为例）
.vscode/         # VS Code 的配置目录
*.swp            # Vim 临时文件
*.swo            # Vim 临时文件
# 环境变量文件（如果存放了密钥、API 等敏感信息）
.env
.env.local
# 日志文件
*.log


# ================
# 4. 例外规则（可选）
# ================
# 比如：虽然忽略了 drafts/ 目录，但想单独保留 drafts/important.md
# !drafts/important.md